[tool.poetry]
name = "demo-service"
version = "0.6.0"
description = "Real-GitHub demo of the deca-pages-api tag-based release train"
authors = ["Engineers <dev@resola.ai>"]

[tool.poetry.dependencies]
python = "^3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
